# Dabble (archived)

Dabble is excluded from the active `normalize_all()` pipeline. Code and data live here for reference only (account no longer usable).

- **Parser:** `dabble_parser.py`
- **Master board:** `data/archive/dabble/dabble_master_board.json`
- **Scraper:** `scrapers/dfs/dabble_engine.py` (writes to archive path if run manually)
