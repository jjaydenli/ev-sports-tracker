# Dabble Credentials
DABBLE_USERNAME=your_phone_number
DABBLE_PASSWORD=your_password

# Betr JWT from fantasy.betr.app DevTools → Authorization (no "Bearer " prefix).
# Check expiry: docs/betting_odds/betr.md → Authentication. TODO: automate refresh.
BETR_BEARER_TOKEN=
